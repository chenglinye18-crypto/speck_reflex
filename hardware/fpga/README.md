# FPGA backend — planned

The FPGA target is for cycle-accurate validation of event scheduling, routing, neuron dynamics, and memory behavior. `rtl/`, `hls/`, and `ip/` remain design placeholders; no synthesizable accelerator is implemented.
