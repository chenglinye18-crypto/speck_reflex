# Hardware layer

All physical backends must implement `hardware.interfaces.ReflexHardware` and declare protocol versions and sensor limits before resource acquisition. This tree contains no working driver, firmware, RTL, USB binding, device opening, Flash write, or motor-control code.
