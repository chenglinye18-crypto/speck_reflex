# STM32N6 backend — planned

STM32N6 is a rapid embedded prototype target, not a claim of neuromorphic execution. Planned work is split into `firmware/`, `drivers/`, and `communication/` only after a board, toolchain, electrical interface, and safety review are selected. No firmware or driver exists today.
