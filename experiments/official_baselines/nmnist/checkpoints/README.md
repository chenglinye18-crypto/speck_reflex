# Checkpoints

The functional demo references the official `scnn_mnist.nir` inside the pinned Sinabs v3.1.3 submodule; it does not duplicate that binary here. Locally trained `best.pt`, `last.pt`, and metadata are generated here and ignored by Git until their provenance, model card, size, and license are reviewed.
