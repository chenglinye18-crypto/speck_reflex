# SNN models

Reserved for hardware-neutral SNN reflex models. The existing official N-MNIST/Speck reproduction remains under `src/speck_reflex/official_baseline` until a verified adapter is introduced.
