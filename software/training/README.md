# Training

Future training pipelines must consume canonical event windows, fix random seeds, record dataset provenance, and export a backend-neutral model contract. Existing official-baseline training remains in its historical experiment directory.
